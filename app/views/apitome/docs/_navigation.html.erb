<ul class="nav sidenav">
  <li>
    <% if Apitome.configuration.single_page %>
      <a href="#readme">README</a>
    <% else %>
      <a href="<%= Apitome.configuration.mount_at %>">README</a>
    <% end %>
  </li>
  <% resources.each do |resource| %>
    <li>
      <% if Apitome.configuration.single_page %>
        <a href="#<%= id_for(examples_for(resource).first['link']) %>"><%= resource['name'] %></a>
        <ul class="nav">
          <% examples_for(resource).each do |example| %>
            <li><%= link_to description_for(example), "##{id_for(example['link'])}" %></li>
          <% end %>
        </ul>
      <% else %>
        <li><%= link_to resource['name'], "#{Apitome.configuration.mount_at}/#{resource['examples'].first['link'].gsub(/\.json$/, '')}" %></li>
        <ul class="nav">
          <% examples_for(resource).each do |example| %>
            <li><%= link_to description_for(example), "#{Apitome.configuration.mount_at}/#{example['link'].gsub(/\.json$/, '')}" %></li>
          <% end %>
        </ul>
      <% end %>
    </li>
  <% end %>
</ul>
